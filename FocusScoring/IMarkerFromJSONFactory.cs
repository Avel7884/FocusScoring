/*using System.Collections.Generic;

namespace FocusScoring
{
    public interface IMarkerFromJSONFactory<TTarget>
    {
        Marker<TTarget> CreateNew(string Name, MarkerColour colour, string description, int Score);
        //Marker<TTarget> GetMarkerByName(string name);
        void Delete(Marker<TTarget> marker);
        IEnumerable<Marker<TTarget>> DeserializeAll();
    }
}*/